function code = huffman_v2(freq)
% Esta es la plantilla que usa en el PDF.

L = size(p,2);              % Longitud del alfabeto
code = repmat({''},1,L);    % Vector para guardar los codigos

iter = 0;                   % iteraciones compacta
idx = zeros(1,2)            % Matriz para guardar los cambios por iter

while 


end

